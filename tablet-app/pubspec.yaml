name: tabletop_ordering_app
description: "A Flutter tabletop ordering and advertising kiosk app."
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  grpc: ^5.1.0
  protobuf: ^6.0.0
  video_player: ^2.8.2
  qr_flutter: ^4.1.0
  http: ^1.1.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.0
  fixnum: ^1.1.0
  path_provider: ^2.1.2
  flutter_svg: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true

  assets:
    - assets/SplashScreen.png
    - assets/digiads-icon.svg
    - assets/digiads-logo.svg
